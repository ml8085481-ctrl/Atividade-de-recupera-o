programa {
  funcao inicio() {
    inteiro dias, meses, anos , total 
    escreva("anos")
    leia(anos)
    escreva("meses ")
    leia(meses)
    escreva("dias ")
    leia(dias)
    total = (anos*365 + meses*30 + dias)
    escreva("total em dias", total)
  }
}
