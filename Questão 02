programa {
  funcao inicio() {
    real A, B, C, R, S, D
    escreva("digite A")
    leia(A)
    escreva("digite B")
    leia(B)
    escreva("digite C")
    leia(C)
    R = (A + B) * (A + B)
    S = (B + C) * (B + C)
    D = (R + S)/2
    escreva("o resultado de D", D)
  }
}
